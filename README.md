# edit_register.nvim

## Features

- Edit registers
- Edit macros (as the are stored in registers)

## Installation

```lua
```

## Default Options

The default options are:
```lua
```

## Usage

Run `require('edit_register').edit_registers()` to edit registers in a seperate buffer.
Save the buffer to write the changes to the registers.
